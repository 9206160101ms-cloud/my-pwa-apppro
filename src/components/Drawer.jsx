import {useApp}from '../context/AppContext';import{tr}from '../lib/i18n';export default function Drawer({open,onClose}){const{setActiveTab,lang,currentTheme:t}=useApp();if(!open)return null;const entries=[['dashboard','home'],['ledger','ledger'],['income','income'],['invoices','invoices'],['management','management'],['inventoryReport','inventory'],['settings','settings']];return <div className="fixed inset-0 z-50 bg-black/40 fade"><button className="absolute inset-0" onClick={onClose}/><aside className="relative h-full w-72 p-5 shadow-2xl" style={{background:t.cardBg,color:t.text}}><div className="flex justify-between items-center mb-8"><b className="text-xl">{tr(lang,'appName')}</b><button onClick={onClose}>✕</button></div>{entries.map(([id,label])=><button key={id} onClick={()=>{setActiveTab(id);onClose()}} className="w-full text-right p-3 rounded-xl hover:bg-slate-500/10">{tr(lang,label)}</button>)}</aside></div>}
